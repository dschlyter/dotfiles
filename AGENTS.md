# Dotfiles repo

Contains configuration and utilities used across machines

- A bunch of configuration files are in the repo, mostly at top level
- `./install.sh` will install/sync them, mostly by symlinking to the home dir
- The `bin` directory contains a bunch of utilities and commands
