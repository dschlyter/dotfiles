#!/bin/bash

if has apt; then
    alias aptu="sudo apt update && sudo apt upgrade && sudo apt-get autoremove --purge && sudo apt-get clean"
fi

has exo-open && alias open="exo-open"

# alias wr="sudo winboot && xfce4-session-logout --reboot"

# disable ctrl-S breaking terminals
stty -ixon
