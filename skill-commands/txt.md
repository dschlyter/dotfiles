---
name: txt
description: Reformat the output as sharable text
---

Some data was just presented in the form of a table or markdown formatted list.

Reformat this data in a easily copy-pastable plain-text format, such as a plain text list.

## Markdown

If the user specifies `markdown`, then output the recent data in markdown but inside a code block. This prevents claude code cli from formatting the output.

Render any tables as markdown tables, and not just as lists in this mode.